
generator client {
  provider = "prisma-client"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}
model Tasks {
  id Int @id @default (autoincrement())
  task String
  done Boolean
}